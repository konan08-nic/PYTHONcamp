ALPHABET = []
KEY = 4

# Fonction pour chiffrer les message
def encode(message, key):
  pass

# Fonction pour dechiffer
def decode(message, key):
  pass

def test(m):
    print(encode(m, KEY))
    print(decode(m, KEY))

test("HELLO", KEY)
